@echo off
REM Commit is already made. This just pushes to GitHub.
REM Your browser may pop up once to authorize Git — approve it.
git push -u origin main
echo.
echo If it succeeded, refresh the repo page — files should appear.
pause
